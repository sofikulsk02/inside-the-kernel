### Persistance is very important in terms of overall goal of the system , we use data and we want that data to be persistantly stored in memory or disks.

that in most of the cases it should be non-volatile
like if power goes or system crashes then the data should servive

## Abstraction:

it is kind of a layer that hides the complexity of the backend task. we have kind of an interface or a high level tool with the help of them you can easily achive your gaol

like if we want to perform certain tasks , we need to write a program for that we can use C language we do not need to know Assembly so the C layer is kind of adding an abstraction, similarly we do not need to know about binary so Assembly is kind of another abstraction layer , then again we do not need to know about transistors then again the logic gates ...
so we have to understand the importance of Abstraction

### Protection

we have to keep in mind that we also have to provide protection the our Os and also to the application --> isolation is the way we can protect the program

### Reliability

we can not afford the system crash the os should run always if the os fails then all the appliction running on the os will also fail, so the os has to be reliable.

### some other factors like energy effeciency , security and mobility

we have small small devices that has os so mobility is also important


### Change in the workflow from 6/12/2026
